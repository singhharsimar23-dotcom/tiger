[project]
name = "tiger"
version = "0.1.0"
dependencies = [
    "fastapi>=0.100.0",
    "jinja2>=3.1.2",
    "python-multipart>=0.0.6",
    "uvicorn>=0.22.0"
]

[tool.vercel]
entrypoint = "dashboard.app:app"
